name: Helix Validation

on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Fork placeholder (docker-golden-solution not used here)
        run: echo "Helix docker validation skipped on this fork."
