name: Build and Push to GAR

on:
  push:
    branches: [master, main]

permissions:
  contents: read

jobs:
  skipped-on-fork:
    runs-on: ubuntu-latest
    steps:
      - name: Skip GAR publish on fork
        run: echo "GAR publish skipped."
