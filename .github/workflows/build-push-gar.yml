name: Build and Push to GAR

on:
  push:
    branches: [master, main]

permissions:
  contents: read

jobs:
  skipped-on-fork:
    runs-on: ubuntu-latest
    steps:
      - name: Skip GAR publish
        run: echo "Artifact registry publish is disabled on this fork."
